require "spec_helper"

describe ChicagoGraffitiReport do
  it "has a version number" do
    expect(ChicagoGraffitiReport::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
